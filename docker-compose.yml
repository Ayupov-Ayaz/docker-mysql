version: '3'

services:
  db:
    image: mysql
    ports:
      - ${MYSQL_PORT}:3306
    env_file:
      .env
    restart: always
    volumes:
      - db_data:/var/lib/mysql
    container_name: mysql

volumes:
  db_data: